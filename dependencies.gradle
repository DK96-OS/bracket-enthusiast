ext {
    find_bugs = "com.google.code.findbugs:jsr305:3.0.2"

    def mt_version = "0.7.1"
    mathtools = [
        pairs : "io.github.dk96-os:pairs:$mt_version",
        numbers : "io.github.dk96-os:numbers:$mt_version"
    ]
}